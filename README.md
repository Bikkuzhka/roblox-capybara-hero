# 🐹 Capybara Hero (Roblox)

Небольшая аркада на Roblox: капибара лопает пузыри булавкой и собирает очки.  
Игра рассчитана на одного игрока, с фиксированной камерой и адаптивным интерфейсом.

---

## 🎮 Идея и геймплей

Из четырёх облаков вылетают пузыри с разными предметами. Облака — спавн-зоны, кольца — зоны поимки, всё привязано к краям экрана и выглядит одинаково на разных разрешениях.   

Игрок управляет капибарой с клавиатуры:

- **Q / E / A / D** - лопнуть пузырь в одной из четырёх зон (верхний левый, верхний правый, нижний левый, нижний правый).   
- При нажатии проигрываются анимации капибары (удар булавкой).   

### Типы пузырей

Каждый пузырь содержит предмет и принадлежит к одной из категорий:   

- `Good` - обычные хорошие предметы, дают **+1 очко**.
- `Bad` - плохие предметы, при поимке **штрафуют по очкам и отнимают жизнь**.
- `Rare` - редкие предметы, дают **усиленный бонус**.
- `Collectible` - коллекционные предметы, дают **ещё больше очков** и сохраняются в прогрессе игрока.
- `Booster` - особые предметы-бустеры:
  - ускоряют или замедляют спавн пузырей;
  - включают магнит, автоматически лопающий пузыри в зоне.   
- `Empty` - пустые пузыри, не делают ничего.

За пропуск хорошего пузыря снимается одна жизнь. Когда жизни заканчиваются, появляется экран **Game Over**, играет звук, показывается финальный счёт и кнопка рестарта.   

---

## ❤️ Интерфейс

Интерфейс полностью адаптивен и поддерживает разные соотношения сторон:

- **Счёт (Score)** - текстовая метка в верхней части экрана, обновляется через `IntValue` в `GuiState`.   
- **Жизни** - до 5 сердечек, скрываются/показываются в зависимости от текущего количества жизней и состояния Game Over.   
- **Таймер бустера** — надпись с типом бустера (`Speed`, `Slow`, `Magnet`) и обратным отсчётом, с пульсирующей анимацией через `TweenService`.   
- **Экран Game Over**:
  - затемнение фона;
  - финальный счёт;
  - кнопка **Restart**;
  - кнопка открытия лидерборда.
- **Лидерборд** — отдельный фрейм с прокручиваемым списком топ-игроков.

---

## 🧠 Техническая часть

Проект построен вокруг чёткого разделения **клиентской логики**, **UI** и **серверных скриптов**.

### Основные скрипты

- `EdgeObjectPlacer.lua` -  
  главный клиентский скрипт игры: спавнит пузыри из облаков, двигает их к зонам, обрабатывает попадания/промахи, бустеры и штрафы по жизням.   

- `MainUIScript.lua` -  
  управление всем UI: счёт, сердечки, экран Game Over, кнопка рестарта, таймер бустера, лидерборд. Состояние хранится в `player.GuiState` через `IntValue`/`BoolValue`/`NumberValue`.   

- `ScoreSave.lua` -  
  серверный скрипт для сохранения рекордов в `DataStore` и выдачи топ-10 в лидерборд. Использует `RemoteEvent`/`RemoteFunction` (`UpdateScore`, `GameOver`, `GetLeaderboard`).   

- `HumAnim.lua` -  
  воспроизведение анимаций капибары при нажатии Q/E/A/D.   

- `CameraLock.lua` -  
  фиксирует камеру на сцене с капибарой и игровым полем.

- `DeleteHumUI.lua` и `HintPlayer.lua` -  
  отключают дефолтный Roblox-интерфейс и скрывают стандартного персонажа, чтобы на сцене оставалась только капибара-герой.   

---

## ▶️ Как запустить

1. Склонируй репозиторий:

   ```bash
   git clone https://github.com/<your-username>/roblox-capybara-hero.git
2. Открой .rbxl / .rbxlx файл проекта в Roblox Studio.

3. Убедись, что в ReplicatedStorage находятся:

   - модель BubbleTemplate;

   - папка BubbleItems с подпапками Good, Bad, Rare, Boosters, Collectibles, Empty.

4. Нажми Play (F5) в Studio, чтобы протестировать игру.

5. Для полноценной работы лидерборда и коллекций включи доступ к DataStore в настройках игры (при публикации в Roblox).
